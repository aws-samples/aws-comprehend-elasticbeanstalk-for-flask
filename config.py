DEBUG=True
UPLOAD_FOLDER = 'userData/'

# If running app on a remote host, access keys can be stored as 
# environment variables to avoid being explicitly defined in the config
# import os
# AWS_ACCESS_KEY = os.environ.get("AWS_ACCESS_KEY")
# AWS_SECRET_KEY = os.environ.get("AWS_SECRET_KEY")